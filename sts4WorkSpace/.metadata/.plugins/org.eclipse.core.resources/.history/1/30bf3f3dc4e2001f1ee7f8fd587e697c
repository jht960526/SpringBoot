<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
<h1>타임리프사용하기</h1>
<p th:text="${myname}">여기는 X</p>
<p th:text="${myname}">여기는 X</p>
<h2>Board정보</h2>
<p th:text = "${board.title}"></p>
<p th:text = "${board.content}"></p>
<p th:text = "${board.writer}"></p>
<p th:text = "${'<h1>'+board.writer+'</h1>'}">HTML tag적용됨</p>
<p th:text = "${'<h1>'+board.writer+'</h1>'}">HTML tag가 그대로 출력</p>
</body>
</html>